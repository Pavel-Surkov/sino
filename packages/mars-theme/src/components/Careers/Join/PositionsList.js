import React from "react";
import { connect, styled } from "frontity";

const PositionsList = () => {
  return <Wrapper>PositionsTable</Wrapper>;
};

const Wrapper = styled.div``;

export default connect(PositionsList);
